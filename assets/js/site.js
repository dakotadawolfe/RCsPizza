/**
 * RC's NYC Pizza & Pasta - Global site script
 */
(function () {
  'use strict';

  // Optional: mobile nav toggle if you add a hamburger later
  var nav = document.querySelector('.nav');
  if (nav && window.matchMedia('(max-width: 639px)').matches) {
    // Could add toggle button here if needed
  }
})();
